import React from 'react';
import ReactDOM from 'react-dom/client';
import { BreakObject } from './BreakObject';

const root = ReactDOM.createRoot(document.querySelector('#app'));

root.render(
  <React.StrictMode>
    <BreakObject />
  </React.StrictMode>
);