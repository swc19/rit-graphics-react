import React from 'react';
import ReactDOM from 'react-dom/client';
import { Top8SimpleText } from './Top8SimpleText';

const root = ReactDOM.createRoot(document.querySelector('#app'));

root.render(
  <React.StrictMode>
    <Top8SimpleText />
  </React.StrictMode>
);